# Demo fallback video (if live WhatsApp fails)

Record ~3 minutes covering:

1. **Webhook curl** — paste sale text into `POST /webhooks/whatsapp` (see README).  
2. **Confirmation** — show Redis session / second curl with `YA`, or POS cart + “Salin draft WhatsApp”.  
3. **Dashboard** — KPI + recent transactions + inventory after refresh.  
4. **Recommendations** — Generate + list.  
5. **Export** — trigger SIMKOPDES export + history row.

Keep synthetic data only; no real NIK on screen.
